# API Backend
VITE_API_URL=http://localhost:8000/api

# ===========================================
# SENTRY (remontée des erreurs JS — plan gratuit)
# ===========================================
# Vide = @sentry/vue n'est même pas chargé (import dynamique conditionnel) :
# aucun impact sur le poids du bundle ni sur les performances en local.
# La release est déduite automatiquement de la version de package.json.
VITE_SENTRY_DSN=
